[package]
name = "ehdb-slm-context"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "SLM execution-context events (S1) and the deterministic fold (S2) for noetl AI-driven steps"

[dependencies]
serde.workspace = true
serde_json.workspace = true
